route:
  receiver: webhook-receiver
  group_by: ["alertname", "service"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: webhook-receiver
    webhook_configs:
      - url: http://context-assembly-service:8080/webhook/alert
        send_resolved: true
